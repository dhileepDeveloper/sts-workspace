server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dhileepDeveloper/Config-Server.git
          search-paths: test,best
          default-label: one
          username: dhileepRaja
          password: abc123ABC
